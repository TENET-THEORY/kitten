# Kitten website

Marketing/docs site for the Kitten CLI, generated with:

```bash
./gradlew :app:run --args='new website --package com.kittenmp.website --path . --type ktor'
```

## Run

```bash
./gradlew :ktor:run
```

Then open http://localhost:8080

## AI-generated content

This site was generated by AI. Look for `@ComprehensionDebt` in the Kitten source tree for other unreviewed AI-written code.
